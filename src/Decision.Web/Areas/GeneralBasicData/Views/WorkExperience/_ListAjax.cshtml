@* GeneratePrettyNames: true*@
@model  Decision.ViewModel.WorkExperience.WorkExperienceListViewModel
@foreach (var e in Model.WorkExperiences)
{
    @Html.Partial(MVC.WorkExperience.Views._WorkExperienceItem, e)
}
