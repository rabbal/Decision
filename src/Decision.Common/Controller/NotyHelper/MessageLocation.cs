namespace Decision.Common.Controller.NotyHelper
{
    public enum MessageLocation
    { bottom, bottomCenter, bottomLeft, bottomRight, center, centerLeft, centerRight, inline, top, topCenter, topLeft, topRight }
}
