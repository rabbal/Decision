@* GeneratePrettyNames: true*@

@model Decision.ViewModel.TrainingCenter.TrainingCenterListViewModel
@foreach (var cneter in Model.TrainingCenters)
{
    @Html.Partial(MVC.TrainingCenter.Views._TrainingCenterItem, cneter)
}
